<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Two-step auth flow activities -->
        <!-- Use MainActivity as the launcher so the fragment-based dashboard is the home entry.
             DashboardActivity is kept but marked non-exported; it can be started from the app's debug-only launcher
             activity or via internal navigation for testing. -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher" />

        <activity
            android:name=".auth.EmailAuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".ui.pin.PinLoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.user.AddUserActivity"
            android:exported="false" />

        <!-- Existing main entry - will be the app's main host for fragment navigation. Keep non-exported. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Legacy DashboardActivity removed: the fragment-based dashboard (MainActivity -> MainFragment) is used instead. -->

        <!-- PDF Generation Test Activity -->
        <activity
            android:name=".pdf.test.PdfTestActivity"
            android:exported="false"
            android:label="PDF Test" />



    </application>

</manifest>