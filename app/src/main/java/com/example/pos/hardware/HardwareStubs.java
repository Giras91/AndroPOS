package com.example.pos.hardware;

/**
 * Lightweight stub to satisfy IDE / build path which still expects
 * the legacy package `com.example.pos.hardware` to exist.
 *
 * This file is intentionally minimal; it can be removed once the
 * workspace / project settings are updated to stop referencing the
 * old package.
 */
public final class HardwareStubs {
    // Prevent instantiation
    private HardwareStubs() {}

    // A no-op init used by any lingering references.
    public static void init() { }
}
